import styled from "styled-components";

const Logo = styled.div`
  font-family: "Over the Rainbow", cursive;
  font-size: 3.5rem;
`;

export default function TitleLogo() {
  return <Logo>Picasso</Logo>;
}
